import { TreeNode } from "./01.100";

export class BSTIterator {
    constructor(root: TreeNode | null) {}

    next(): number {
        return 1;
    }

    hasNext(): boolean {
        return false;
    }
}
